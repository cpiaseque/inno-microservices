package com.innowise.authservice.model.entity.enums;

/**
 * Enumeration defining user roles and access levels in the system.
 * Used for role-based authorization and access control.
 */
public enum Role {
    USER, ADMIN, SERVICE
}